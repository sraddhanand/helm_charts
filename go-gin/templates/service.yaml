apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-gin.fullname" . }}
  labels:
    {{- include "go-gin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-gin.selectorLabels" . | nindent 4 }}
